#include <stdio.h>

/**
 * main - program that prints its name in a new line
 * @argv: argument vector
 * @argc: number of arguments
 * Return: 0
 */
int main(char *argv[], int argc)
{
	printf("%s\n", argv[0]);

	return (0);
}
